import 'package:flutter/material.dart';
import 'package:flutter_sports/resources/strings.dart';

class CalendrPage extends StatelessWidget {
  const CalendrPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text(Strings.calendar),
      
    );
  }
}